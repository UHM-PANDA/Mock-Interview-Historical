#ifndef TRIPLESUMHPP
#define TRIPLESUM

#include<vector>

bool triple_sum_brute(std::vector<int> input, int a);
bool triple_sum_optimal(std::vector<int> a, int target);
bool two_sum(std::vector<int> input, int a);

#endif